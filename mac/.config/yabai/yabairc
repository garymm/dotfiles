# Default to tiled
yabai -m config layout bsp
# Show shadows only for floating windows
yabai -m config window_shadow float

# float certain apps
yabai -m rule --add app="^System Preferences$" manage=off
yabai -m rule --add app="^Preferencias del Sistema$" manage=off
yabai -m rule --add app="^Stats$" manage=off
yabai -m rule --add app="^Timer$" manage=off
yabai -m signal --add event=window_created app="^1Password$" title="^$" action=~/.config/yabai/focus_1password.sh

# use the mouse
yabai -m config mouse_modifier ctrl
yabai -m config mouse_action1 resize
yabai -m config mouse_action2 move

# create 8 spaces
# TODO: Currently doing this manually on each boot because it requires SIP to be disabled.
# Hopefully I can find a way to make this work with SIP enabled.
# https://apple.stackexchange.com/questions/234606/create-a-new-space-using-a-keyboard-shortcut
# ~/.config/yabai/manage_spaces.sh init 8
